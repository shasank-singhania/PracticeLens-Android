name: CI

on:
  pull_request:
  push:
    branches: [ main, codex/practicelens-installable-drive-distribution ]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
      - run: python3 tools/structural_guard.py
      - run: ./gradlew --version
      - run: ./gradlew testDemoDebugUnitTest
      - run: ./gradlew lintDemoDebug
      - run: ./gradlew assembleDemoDebug
      - uses: actions/upload-artifact@v4
        with:
          name: practicelens-demo-debug-apk
          path: app/build/outputs/apk/demo/debug/*.apk
